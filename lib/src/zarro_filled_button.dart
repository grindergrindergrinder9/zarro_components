import 'package:flutter/widgets.dart';

class ZarroFilledButton extends StatelessWidget {
  const ZarroFilledButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
